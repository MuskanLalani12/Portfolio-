# Professional Portfolio Website

A responsive, accessible, and animated personal portfolio website built with HTML, CSS, and JavaScript.

## 🚀 Features

- **Responsive Design**: Works on Mobile, Tablet, and Desktop.
- **Dark Mode Aesthetic**: sleek modern look with teal accents.
- **Smooth Animations**: Scroll reveal, hover effects, and page transitions.
- **Projects Gallery**: Filterable grid for showcasing work.
- **Accessibility**: Semantic HTML, ARIA labels, and keyboard navigation support.
- **Contact Form**: Client-side validated form with error handling.

## 🛠️ Setup Instructions

No build process is required! This is a static site.

1.  **Clone or Download** the repository.
2.  **Open** `index.html` in your web browser.
    *   For the best experience, use a local development server to ensure all assets load correctly (e.g., VS Code "Live Server" extension).
    *   If using Python: `python -m http.server 8000`

## 📁 File Structure

```
/
├── index.html      # Main HTML structure
├── styles.css      # All styling and animations
├── script.js       # Logic for interactions (animations, menu, filter)
└── README.md       # This file
```

## 🎨 Customization

-   **Colors**: Edit the CSS Variables in `styles.css` (`:root`).
-   **Content**: Update text in `index.html`.
-   **Images**: Add your own images to an `assets/` folder and update source paths in HTML.

## 📄 License
MIT
